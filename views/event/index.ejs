<%- include('../partials/header.ejs')%>
  <div class = "content">
        <h1>Events</h1> 
        <%if (events.length){%>
            <p>Select an event for details</p>
            <%events.forEach(event => {%>
               <a href="/events/<%= event.name%>"> <p><%=event.name%></p></a>
            <%});
        } else{%>
            <p>There are no stories to display.</p>
            <%}%>
       </div> 
    <%- include('../partials/footer.ejs')%>
